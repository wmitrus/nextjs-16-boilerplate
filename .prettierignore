.next
out
build
next-env.d.ts
node_modules
package-lock.json
pnpm-lock.yaml
public

# Historical task archives and other AI-tool session state (OZI-59): not
# authored documentation, reformatting them has no purpose and would produce
# a huge one-time diff the moment markdown formatting actually runs (lint,
# on-save, lint-staged) instead of being silently skipped like before.
.copilot
.zencoder
.zenflow

# Generated by @semantic-release/changelog on every release; reformatting it
# by hand just churns against whatever the release tool writes next time.
CHANGELOG.md
