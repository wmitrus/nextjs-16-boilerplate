# Scanner Ignore Report — Feature Flags Security Audit

This report classifies all findings from the feature-flags security audit for scanner or finding-management tools.

| File                                                                                  | Line                  | Rule / Finding                                                 | Classification                   | Action                     | Rationale                                                                                                                |
| ------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------- | -------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `src/modules/feature-flags/infrastructure/growthbook/GrowthBookFeatureFlagService.ts` | 10–25                 | Cross-tenant data contamination (shared mutable state)         | **Real risk — CRITICAL**         | Fix required (CRIT-01)     | Singleton with per-request mutable `setAttributes()` call; must be redesigned to isolate request context                 |
| `src/security/middleware/route-policy.ts`                                             | PUBLIC_ROUTE_PREFIXES | Auth enforcement mismatch                                      | **Real risk — CRITICAL**         | Fix required (CRIT-02)     | `/feature-flags-demo` missing from public routes; E2E spec invalid against production config                             |
| `scripts/flags/export.ts`                                                             | 59, 72                | CWE-22 Path Traversal — `fs.writeFileSync` with CLI arg path   | **Real risk — MAJOR**            | Fix required (MAJ-01)      | `--out` arg flows to `fs.writeFileSync` without `path.resolve()` or confinement check                                    |
| `scripts/flags/import.ts`                                                             | 20, 24                | CWE-22 Path Traversal — `fs.readFileSync` with CLI arg path    | **Real risk — MAJOR**            | Fix required (MAJ-02)      | `--file` arg flows to `fs.readFileSync` without confinement check                                                        |
| `src/modules/feature-flags/factory.ts`                                                | 55                    | CWE-918 SSRF — env-var URL to HTTP client                      | **Real risk — MAJOR**            | Fix required (MAJ-03)      | `GROWTHBOOK_API_HOST` passed to SDK without point-of-use protocol/hostname allowlist                                     |
| `src/modules/feature-flags/infrastructure/resilient/ResilientFeatureFlagService.ts`   | 21–28                 | Sensitive data in logs — raw error object                      | **Real risk — MAJOR**            | Fix required (MAJ-04)      | `error` object may contain DB connection strings; sanitize to `errorMessage`/`errorName`                                 |
| `src/modules/feature-flags/factory.ts`                                                | default branch        | `console.warn` instead of structured logger                    | **Real risk — MINOR**            | Fix in follow-up           | Security-relevant config fallback not captured in structured observability                                               |
| `scripts/flags/import.ts`                                                             | 29                    | `JSON.parse(raw) as FlagsFile` without schema validation       | **Real risk — MINOR**            | Fix in follow-up           | Type assertion only; no runtime Zod validation of file content before DB import                                          |
| `scripts/flags/export.ts`                                                             | end of file           | No `isMain` guard                                              | **Real risk — MINOR**            | Fix in follow-up           | Consistent with Pattern D in AGENTS.md; low immediate risk since file exports nothing                                    |
| `scripts/flags/import.ts`                                                             | end of file           | No `isMain` guard                                              | **Real risk — MINOR**            | Fix in follow-up           | Same as above                                                                                                            |
| `src/modules/feature-flags/infrastructure/drizzle/schema.ts`                          | tenantId column       | UUID type for external ID (was: initial schema)                | **Fixed — resolved**             | Already resolved (Step 17) | Changed from `uuid` to `text`; migration `0007` generated                                                                |
| `src/modules/feature-flags/infrastructure/drizzle/schema.ts`                          | unique constraint     | `uniqueIndex` without `nullsNotDistinct` (was: initial schema) | **Fixed — resolved**             | Already resolved (Step 17) | Changed to `unique().nullsNotDistinct()`                                                                                 |
| `src/app/feature-flags-demo/page.tsx`                                                 | `demoAuthContext`     | Synthetic context with hardcoded values                        | **False positive — intentional** | No action                  | Demo page explicitly uses synthetic context (documented). Not a real user identity claim.                                |
| `src/modules/feature-flags/lib/isFeatureEnabled.ts`                                   | entire file           | Thin wrapper with no auth check                                | **False positive**               | No action                  | This is a helper utility — auth context is the caller's responsibility. The contract signature enforces context passing. |

---

## Summary

- **Must fix (CRITICAL/MAJOR)**: 6 findings
- **Follow-up (MINOR)**: 4 findings
- **Already fixed**: 2 findings
- **False positives / intentional**: 2 findings
